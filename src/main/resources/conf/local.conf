conf {
  cassandra {
    hostnames = ["localhost"]
    port = 9042
    ssl = false
    admin_keyspace = "keyspace_administration"
    keyspaces = ["keyspace1", "keyspace2"]
    username=adm
    password=password
    app_version="v0"
  }
}
